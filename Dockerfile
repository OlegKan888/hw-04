# Використання офіційного Python-образу як базового
FROM python:3.12-slim

# Встановлення робочої директорії
WORKDIR /app

# Копіювання файлів програми в контейнер
COPY . /app

# Встановлення необхідних залежностей
RUN pip install --no-cache-dir Flask

# Створення каталогу для зберігання даних
RUN mkdir -p /app/storage

# Вказати порт, який буде відкрито в контейнері
EXPOSE 3000

# Запуск програми
CMD ["python", "main.py"]
